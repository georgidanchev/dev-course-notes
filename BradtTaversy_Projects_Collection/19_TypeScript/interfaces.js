{
    function showTodo(todo) {
        console.log(todo.title + ' ' + todo.text);
    }
    var myTodo1 = {
        title: 'Trash',
        text: 'Take out trash'
    };
    // showTodo(myTodo);
}
{
    function showTodo(todo) {
        console.log(todo.title + ' ' + todo.text);
    }
    var myTodo2 = {
        title: 'Trash',
        text: 'Take out trash'
    };
    showTodo(myTodo2);
}
